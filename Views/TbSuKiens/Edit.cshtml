@model API.Sparta.Models.TbSuKien

@{
    ViewData["Title"] = "Edit";
}

<div class="content">
    <div class="animated fadeIn">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <a asp-action="Index" class="btn btn-sm btn-primary"><i class="fa fa-reply"></i></a><strong>Quản lý sự kiện</strong>
                    </div>
                    <form asp-action="Edit" asp-antiforgery="true" enctype="multipart/form-data">
                        <div class="card-body card-block">
                            <div class="form-horizontal">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <input type="hidden" asp-for="SukienId" />
                                <div class="form-group">
                                    <label asp-for="SukienDiachi" class="control-label">Địa điểm</label>
                                    <input asp-for="SukienDiachi" class="form-control" />
                                    <span asp-validation-for="SukienDiachi" class="text-danger"></span>
                                </div>
                                <div class="form-group">
                                    <label asp-for="SukienNgaygio" class="control-label">Thời gian</label>
                                    <input type="date" asp-for="SukienNgaygio" class="form-control" />
                                    <span asp-validation-for="SukienNgaygio" class="text-danger"></span>
                                </div>
                                <div class="form-group">
                                    <label asp-for="SukienMota" for="textarea-input" class=" form-control-label">Mô tả</label>
                                    <textarea asp-for="SukienMota" class="form-control" rows="9"></textarea>
                                    @*<textarea asp-for="CamnangNoidung" name="textarea-input" id="textarea-input" rows="9" placeholder="Editor Content..." class="form-control"></textarea>*@
                                    <span asp-validation-for="SukienMota" class="text-danger"></span>
                                    @*<div class="form-group btn btn-primary">
                                            <input type="submit" value="Save" class="btn btn-primary" />
                                        </div>*@
                                </div>
                                <div class="card-footer text-right">
                                    <div class="form-group">
                                        <button type="submit" class="btn btn-primary" value="Save">
                                            <i class="fa fa-save"></i>Lưu
                                        </button>
                                        <button type="reset" class="btn btn-danger">
                                            <i class="fa fa-undo"></i>Hủy
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
